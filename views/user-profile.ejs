<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head') %>
        <title>User Profile</title>
        <style>
            body {
                display: flex;
                flex-direction: column;
                min-height: 100vh;
                margin: 0;
                font-family: Arial, sans-serif;
                background-color: #f4f4f9;
            }

            header {
                background: linear-gradient(45deg, #6a11cb, #2575fc);
                color: white;
                padding: 20px;
                text-align: center;
            }

            header h1 {
                margin: 0;
                font-size: 2rem;
            }

            .container {
                flex: 1;
                padding: 20px;
                max-width: 900px;
                margin: 0 auto;
            }

            .profile {
                display: flex;
                flex-wrap: wrap;
                justify-content: space-between;
                align-items: center;
                background: #fff;
                padding: 20px;
                border-radius: 8px;
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            }

            .profile-picture {
                text-align: center;
                margin-bottom: 20px;
                flex: 1 1 200px;
            }

            .profile-picture img {
                border-radius: 50%;
                width: 150px;
                height: 150px;
                object-fit: cover;
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            }

            .user-info {
                flex: 2 1 400px;
                padding: 20px;
            }

            .user-info h2 {
                margin: 0 0 10px;
                font-size: 1.8rem;
                color: #333;
            }

            .user-info p {
                margin: 8px 0;
                font-size: 1rem;
                color: #555;
            }

            footer {
                background: #333;
                color: #fff;
                text-align: center;
                padding: 15px;
                position: fixed;
                width: 100%;
                bottom: 0;
            }

            @media (max-width: 768px) {
                .profile {
                    flex-direction: column;
                    text-align: center;
                }

                .user-info {
                    padding: 10px;
                }

                .profile-picture img {
                    width: 120px;
                    height: 120px;
                }
            }
        </style>
</head>

<body>
    <%- include('./partials/nav') %>

        <div class="container">
            <div class="profile">
                <!-- Profile Picture -->
                <div class="profile-picture">
                    <img src="<%= userDetails.profileImage %>" alt="<%= userDetails.fullName %>'s Avatar">
                </div>
                <!-- User Info -->
                <div class="user-info">
                    <h2>
                        <%= userDetails.fullName %>
                    </h2>
                    <p><strong>Email:</strong>
                        <%= userDetails.email %>
                    </p>
                    <p><strong>Account Verified:</strong>
                        <%= userDetails.isVerified ? "Yes" : "No" %>
                    </p>
                    <p><strong>Total Post:</strong>
                        <%= posts %>
                    </p>
                    <p><strong>Joined Date:</strong>
                        <%= new Date(userDetails.createdAt).toLocaleDateString("en-US", { year: "numeric" ,
                            month: "long" , day: "numeric" }) %>
                    </p>
                </div>
            </div>
        </div>

        <footer>
            <p>&copy; 2025 Blogify. All rights reserved.</p>
        </footer>

        <%- include('./partials/script') %>
</body>

</html>